{
  "name": "optimistic-terminal",
  "version": "1.0.0",
  "description": "Real terminal for Optimistic OS",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js"
  },
  "dependencies": {
    "ws": "^8.14.2"
  }
}